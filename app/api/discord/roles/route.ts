import{NextResponse}from'next/server';import{createServerSupabase}from'@/lib/supabase/server';import{botGuildRoles}from'@/lib/discord'
export async function GET(){const sb=await createServerSupabase();const{data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const{data:connection,error}=await sb.from('discord_guild_connections').select('guild_id,guild_name').eq('owner_id',user.id).maybeSingle();if(error)return NextResponse.json({error:error.message},{status:500});if(!connection)return NextResponse.json({connected:false,roles:[]});try{const roles=await botGuildRoles(connection.guild_id);return NextResponse.json({connected:true,guild:connection,roles:roles.filter(r=>r.name!=='@everyone').map(r=>({id:String(r.id),name:String(r.name),color:Number(r.color||0),position:Number(r.position||0),managed:Boolean(r.managed)})).sort((a,b)=>b.position-a.position)})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed to load roles'},{status:502})}}
