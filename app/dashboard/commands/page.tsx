'use client'
import {useEffect,useMemo,useState} from'react';import Link from'next/link';import{useRouter}from'next/navigation';import{createClient}from'@/lib/supabase/client'
type C={id?:string;command_name:string;enabled:boolean;aliases:string[];permissions:string[];global_cooldown_seconds:number;user_cooldown_seconds:number;source:string;plugin_id:string|null;usage_count:number};type A={id:number;created_at:string;twitch_login:string;command_name:string;success:boolean;error_code:string|null;raw_command:string}
const BUILTINS=['font','namefont','fontsize','namesize','color','namecolor','background','opacity','rainbow','glow','animation','theme','density','lifetime','maxmessages','userfont','usercolor','userglow','userreset','rolefont','rolecolor','roleglow','reset','help','plugins'];const PUBLIC=['help','plugins'];const MOD=['color'];const ROLES=['broadcaster','moderator','vip','subscriber','viewer']
export default function Page(){const s=useMemo(()=>createClient(),[]),r=useRouter();const[owner,setOwner]=useState(''),[prefix,setPrefix]=useState('CC!'),[rows,setRows]=useState<C[]>([]),[audit,setAudit]=useState<A[]>([]),[status,setStatus]=useState('Loading…');useEffect(()=>{(async()=>{const{data:{user}}=await s.auth.getUser();if(!user){r.replace('/auth');return}setOwner(user.id);const[o,c,a]=await Promise.all([s.from('overlays').select('command_prefix').eq('user_id',user.id).single(),s.from('command_configurations').select('*').eq('owner_id',user.id).order('command_name'),s.from('command_audit_log').select('id,created_at,twitch_login,command_name,success,error_code,raw_command').eq('owner_id',user.id).order('created_at',{ascending:false}).limit(25)]);setPrefix(o.data?.command_prefix||'CC!');const m=new Map((c.data||[]).map((x:any)=>[x.command_name,x]));setRows(BUILTINS.map(n=>norm(n,m.get(n))).concat((c.data||[]).filter((x:any)=>!BUILTINS.includes(x.command_name)).map((x:any)=>norm(x.command_name,x))));setAudit((a.data||[])as A[]);setStatus('Ready')})()},[r,s]);async function savePrefix(){const v=prefix.trim();if(!v||v.length>8){setStatus('Prefix must be 1–8 chars');return}const{error}=await s.from('overlays').update({command_prefix:v}).eq('user_id',owner);setStatus(error?error.message:'Prefix saved')}async function save(x:C){const{error}=await s.from('command_configurations').upsert({...x,owner_id:owner},{onConflict:'owner_id,command_name'});setStatus(error?error.message:`${prefix}${x.command_name} saved`)}function patch(n:string,p:Partial<C>){setRows(v=>v.map(x=>x.command_name===n?{...x,...p}:x))}return <main className="shell"><nav className="nav"><div className="brand">CTCI <span>Commands</span></div><div className="actions"><Link className="btn" href="/dashboard">Overlay</Link><Link className="btn" href="/dashboard/plugins">Plugins</Link></div></nav><section className="panel"><div className="section-head"><div><h2>Chat command engine</h2><p className="muted">Real Twitch commands with permissions, aliases, distributed cooldowns and audit logging.</p></div><span className="status">{status}</span></div><div className="row"><div className="field"><label>Prefix</label><input value={prefix} maxLength={8} onChange={e=>setPrefix(e.target.value)}/></div><div className="actions"><button className="btn primary" onClick={savePrefix}>Save prefix</button></div></div><div className="command-list">{rows.map(x=><article className="command-card" key={x.command_name}><div className="command-title"><code>{prefix}{x.command_name}</code><span className="small muted">{x.source}{x.plugin_id?` · ${x.plugin_id}`:''} · {x.usage_count} uses</span></div><div className="row"><label className="check"><input type="checkbox" checked={x.enabled} onChange={e=>patch(x.command_name,{enabled:e.target.checked})}/>Enabled</label><div className="field"><label>Aliases</label><input value={x.aliases.join(', ')} onChange={e=>patch(x.command_name,{aliases:e.target.value.split(',').map(v=>v.trim().toLowerCase()).filter(Boolean)})}/></div></div><div className="field"><label>Allowed roles</label><div className="role-grid">{ROLES.map(role=><label className="check" key={role}><input type="checkbox" checked={x.permissions.includes(role)} onChange={e=>patch(x.command_name,{permissions:e.target.checked?[...new Set([...x.permissions,role])]:x.permissions.filter(v=>v!==role)})}/>{role}</label>)}</div></div><div className="row"><div className="field"><label>Global cooldown</label><input type="number" min={0} max={3600} value={x.global_cooldown_seconds} onChange={e=>patch(x.command_name,{global_cooldown_seconds:+e.target.value})}/></div><div className="field"><label>User cooldown</label><input type="number" min={0} max={86400} value={x.user_cooldown_seconds} onChange={e=>patch(x.command_name,{user_cooldown_seconds:+e.target.value})}/></div></div><button className="btn" onClick={()=>save(x)}>Save command</button></article>)}</div><hr className="divider"/><h2>Recent command audit</h2><div className="command-list">{audit.length===0?<p className="muted">No commands used yet.</p>:audit.map(a=><div className="command-card" key={a.id}><div className="command-title"><code>{a.raw_command}</code><span className={a.success?'status':'status danger'}>{a.success?'Success':a.error_code||'Failed'}</span></div><div className="small muted">{new Date(a.created_at).toLocaleString()} · {a.twitch_login} · {a.command_name}</div></div>)}</div></section></main>}
function norm(n:string,row:any):C{const permissions=row?.permissions??(PUBLIC.includes(n)?['viewer','subscriber','vip','moderator','broadcaster']:MOD.includes(n)?['broadcaster','moderator']:['broadcaster']);return{command_name:n,enabled:row?.enabled??true,aliases:row?.aliases??[],permissions,global_cooldown_seconds:row?.global_cooldown_seconds??(n==='help'?2:1),user_cooldown_seconds:row?.user_cooldown_seconds??(n==='help'?10:5),source:row?.source??'core',plugin_id:row?.plugin_id??null,usage_count:Number(row?.usage_count||0),id:row?.id}}
