'use client'
import{useEffect,useMemo,useState}from'react';import Link from'next/link';import{useRouter}from'next/navigation';import{createClient}from'@/lib/supabase/client'
type Badge={id:string;slug:string;name:string;description:string;icon_text:string|null;icon_url:string|null;color:string;is_platform:boolean;owner_id:string|null};type Assign={id:string;badge_id:string;twitch_login:string;source:string};type Rule={id:string;badge_id:string;guild_id:string;role_id:string;enabled:boolean};type DLink={id:string;twitch_login:string;discord_user_id:string}
export default function Page(){const sb=useMemo(()=>createClient(),[]),r=useRouter();const[owner,setOwner]=useState(''),[badges,setBadges]=useState<Badge[]>([]),[assign,setAssign]=useState<Assign[]>([]),[rules,setRules]=useState<Rule[]>([]),[links,setLinks]=useState<DLink[]>([]),[status,setStatus]=useState('Loading…'),[form,setForm]=useState({name:'',slug:'',icon_text:'',icon_url:'',color:'#9147FF'}),[give,setGive]=useState({badge_id:'',twitch_login:''}),[rule,setRule]=useState({badge_id:'',guild_id:'',role_id:''}),[link,setLink]=useState({twitch_login:'',discord_user_id:''});async function load(){const{data:{user}}=await sb.auth.getUser();if(!user){r.replace('/auth');return}setOwner(user.id);const[b,a,ru,li]=await Promise.all([sb.from('badge_definitions').select('*').or(`owner_id.is.null,owner_id.eq.${user.id}`).order('created_at'),sb.from('badge_assignments').select('id,badge_id,twitch_login,source').or(`owner_id.is.null,owner_id.eq.${user.id}`).order('created_at'),sb.from('discord_badge_sync').select('*').eq('owner_id',user.id),sb.from('discord_user_links').select('id,twitch_login,discord_user_id').eq('owner_id',user.id)]);setBadges((b.data||[])as Badge[]);setAssign((a.data||[])as Assign[]);setRules((ru.data||[])as Rule[]);setLinks((li.data||[])as DLink[]);setStatus('Ready')}useEffect(()=>{load()},[]);async function create(){const payload={owner_id:owner,name:form.name.trim(),slug:form.slug.trim().toLowerCase(),icon_text:form.icon_text.trim()||null,icon_url:form.icon_url.trim()||null,color:form.color,is_platform:false};const{error}=await sb.from('badge_definitions').insert(payload);setStatus(error?error.message:'Badge created');if(!error){setForm({name:'',slug:'',icon_text:'',icon_url:'',color:'#9147FF'});await load()}}async function giveBadge(){const{error}=await sb.from('badge_assignments').insert({owner_id:owner,badge_id:give.badge_id,twitch_login:give.twitch_login.trim().toLowerCase(),source:'manual'});setStatus(error?error.message:'Badge assigned');if(!error)await load()}async function addRule(){const{error}=await sb.from('discord_badge_sync').insert({owner_id:owner,badge_id:rule.badge_id,guild_id:rule.guild_id.trim(),role_id:rule.role_id.trim(),enabled:true});setStatus(error?error.message:'Discord role sync added');if(!error)await load()}async function addLink(){const{error}=await sb.from('discord_user_links').upsert({owner_id:owner,twitch_login:link.twitch_login.trim().toLowerCase(),discord_user_id:link.discord_user_id.trim()},{onConflict:'owner_id,twitch_login'});setStatus(error?error.message:'Discord user link saved');if(!error)await load()}async function sync(){setStatus('Syncing Discord roles…');const res=await fetch('/api/badges/discord-sync',{method:'POST'}),body=await res.json();setStatus(res.ok?`Sync complete: ${body.granted} granted, ${body.removed} removed`:(body.error||'Sync failed'));if(res.ok)await load()}return <main className="shell"><nav className="nav"><div className="brand">CTCI <span>Badges</span></div><div className="actions"><Link className="btn" href="/dashboard">Overlay</Link><Link className="btn" href="/dashboard/commands">Commands</Link><Link className="btn" href="/dashboard/plugins">Plugins</Link></div></nav><section className="panel"><div className="section-head"><div><h2>Custom badges</h2><p className="muted">Platform badges appear globally. Streamer badges are scoped to your CTCI chat.</p></div><span className="status">{status}</span></div><div className="row"><div className="field"><label>Name</label><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="field"><label>Slug</label><input value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})}/></div></div><div className="row"><div className="field"><label>Badge text</label><input maxLength={12} value={form.icon_text} onChange={e=>setForm({...form,icon_text:e.target.value})} placeholder="VIP"/></div><div className="field"><label>Color</label><input type="color" value={form.color} onChange={e=>setForm({...form,color:e.target.value})}/></div></div><div className="field"><label>Icon URL (optional)</label><input value={form.icon_url} onChange={e=>setForm({...form,icon_url:e.target.value})}/></div><button className="btn primary" onClick={create}>Create badge</button><hr className="divider"/><h2>Available badges</h2><div className="plugin-list">{badges.map(b=><div className="plugin-card" key={b.id}><div className="plugin-title"><strong>{b.name}</strong><span className="custom-badge" style={{color:b.color,borderColor:b.color}}>{b.icon_text||b.slug}</span></div><div className="small muted">{b.is_platform?'Platform badge':'Custom badge'} · {b.slug}</div></div>)}</div><hr className="divider"/><h2>Assign badge</h2><div className="row"><div className="field"><label>Badge</label><select value={give.badge_id} onChange={e=>setGive({...give,badge_id:e.target.value})}><option value="">Select…</option>{badges.filter(b=>!b.is_platform).map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></div><div className="field"><label>Twitch login</label><input value={give.twitch_login} onChange={e=>setGive({...give,twitch_login:e.target.value})}/></div></div><button className="btn" onClick={giveBadge}>Assign</button><hr className="divider"/><h2>Discord role sync</h2><p className="muted">Link a Twitch username to a Discord user ID, then map a Discord guild role to one of your badges.</p><div className="row"><div className="field"><label>Twitch login</label><input value={link.twitch_login} onChange={e=>setLink({...link,twitch_login:e.target.value})}/></div><div className="field"><label>Discord user ID</label><input value={link.discord_user_id} onChange={e=>setLink({...link,discord_user_id:e.target.value})}/></div></div><button className="btn" onClick={addLink}>Save user link</button><div className="row"><div className="field"><label>Badge</label><select value={rule.badge_id} onChange={e=>setRule({...rule,badge_id:e.target.value})}><option value="">Select…</option>{badges.filter(b=>!b.is_platform).map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></div><div className="field"><label>Discord guild ID</label><input value={rule.guild_id} onChange={e=>setRule({...rule,guild_id:e.target.value})}/></div></div><div className="field"><label>Discord role ID</label><input value={rule.role_id} onChange={e=>setRule({...rule,role_id:e.target.value})}/></div><div className="actions"><button className="btn" onClick={addRule}>Add sync rule</button><button className="btn primary" onClick={sync}>Sync Discord now</button></div><div className="small muted">Configured links: {links.length} · Sync rules: {rules.length} · Assignments: {assign.length}</div></section></main>}
